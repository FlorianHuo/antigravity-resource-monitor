{
  "name": "antigravity-resource-monitor",
  "displayName": "Resource Monitor",
  "description": "Monitor and display per-window memory usage in the status bar",
  "version": "0.1.0",
  "publisher": "florian",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "resourceMonitor.showDetails",
        "title": "Resource Monitor: Show Memory Details"
      },
      {
        "command": "resourceMonitor.toggle",
        "title": "Resource Monitor: Toggle Visibility"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
